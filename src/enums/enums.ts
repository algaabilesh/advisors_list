export enum SortingOptions {
  ASC = "ASC",
  DESC = "DESC",
}

export enum FilterOptionsByStatus {
  ALL = "ALL",
  ONLINE = "ONLINE",
  OFFLINE = "OFFLINE",
}

export enum LanguageOptions {
  ALL = "ALL",
  EN = "English",
  FR = "French",
  ES = "Spanish",
  DE = "German",
}
